//! `VRM` avatar rendering for Lana via Bevy and [`bevy_vrm`].
//!
//! Loads the `lana.vrm` model, runs idle animations and applies blendshape
//! targets received from `lana-viseme` at 60 fps. Implemented in Phase 5.

#![forbid(unsafe_code)]
